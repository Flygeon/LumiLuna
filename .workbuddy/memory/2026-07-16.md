# 2026-07-16

## 项目目标升级：Demo → 可上线产品
- 用户提出要把 LumiLuna 做成「实际可交付、可上线」的项目，问还差什么。
- 核查（已确认）：仓库**无 LICENSE**、**无 test/**、**无 i18n/arb**；`windows/` 目录不入库（CI 里 `flutter create --platforms=windows` 生成）；`pubspec` 版本 `1.0.0+1` 正常。
- 用户选择「先出落地计划文档，暂不写代码」。

## 产出：DELIVERY_ROADMAP.md
- 已写 `DELIVERY_ROADMAP.md`：分 M0(上线硬门槛)/M1(产品完善)/M2(工程化)/M3(可选增强) 四个里程碑，每项标注改动文件 + 验收标准。
- M0 关键项：自定义图标、安装包+代码签名(Inno Setup/signtool)、LICENSE、全局错误兜底(runZonedGuarded + ErrorWidget.builder)。
- M1 关键项：音频元数据(metadata_god)、文件删除/收藏、i18n、设置缓存清理/关于页、规模化性能(图片缓存上限+扫描持久化)、键盘/无障碍。
- M2：单元测试、CI 增强(跑 test→Release→签名→按 tag 发 Release)、CHANGELOG/CONTRIBUTING。
- 文档中明确了 3 个待拍板决策：①windows/ 提交 vs CI 注入图标版本；②代码签名证书来源(自签仍被 SmartScreen 拦)；③分发渠道(GitHub Releases/Store/直链)。建议提交 windows/ + 正规 CA 签名 + GitHub Releases。

## 待用户决策后实现
- 路线图已供评审；等用户选定里程碑/范围后再动手写代码。
